apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-base-devops
  namespace: devops
data:
  USER: "Mariano Marchant"
---
apiVersion: v1
kind: Secret
metadata:
  name: backend-base-devops
  namespace: devops
type: Opaque
data:
  API_KEY: QUFBREtFWTNFU1NFVFI= #APIKEYSECRET
---    
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-base-devops-deployment
  namespace: devops
  labels:
    app: backend-base-devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-base-devops
  template:
    metadata:
      labels:
        app: backend-base-devops
    spec:
      containers:
      - name: backend-base-devops
        image: localhost:32775/backend-base-devops:main-98
        imagePullPolicy: Always
        env:
        - name: USER
          valueFrom:
            configMapKeyRef:
              name: backend-base-devops
              key: USER
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: backend-base-devops
              key: API_KEY
        ports:
        - containerPort: 3001
      imagePullSecrets:
      - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: backend-base-devops-svc
  namespace: devops
  labels:
    app: backend-base-devops
spec:
  selector:
    app: backend-base-devops
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-base-devops-ingress
  namespace: devops
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/disable-admission-validation: "true"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /proyecto(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: backend-base-devops-svc
            port:
              number: 80